const initialState = [
    {
        id: 1,
        label: "USA",
    },
    {
        id: 2,
        label: "Russia",
    },
];

export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}